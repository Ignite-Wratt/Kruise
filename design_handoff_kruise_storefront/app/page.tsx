import React from 'react';
import { KruiseV2Home } from '../components/KruiseV2Home';

// v2 drop-culture home, rebuilt from the design system's `Kruise Storefront v2.html`.
// Editorial content is the design reference's; wiring the rail/hero/drop to live
// Shopify products (getProducts) is a follow-up — see SYNC_REPORT.md.

export default function HomePage() {
  return <KruiseV2Home />;
}
